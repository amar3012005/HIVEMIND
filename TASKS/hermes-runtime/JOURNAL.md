# JOURNAL — hermes-runtime (append-only)

- 2026-06-06 · PLAN · Fetched all 4 Hermes docs (profiles, profile-distributions, multi-profile-gateways, docker) via web; recon = greenfield (no existing Hermes code; hm-control = control-plane-server.js ~6793 lines; no hm-hermes compose service). Authored README (vision, acceptance criteria, Hermes reference, JSON config contract, 6-phase plan, safety) + STATE + this JOURNAL. Registered durable cron. Verdict: GREEN — ready for Phase 1.
- 2026-06-06 · UPGRADE · task-cron skill bumped to v2 (CONTINUOUS, no fixed interval). Retired 30-min cron f82b0b00; switched runner to continuous self-paced (ScheduleWakeup 60s resume, finish+verify→next immediately). Task NOT restarted — README/phases/recon/STATE phase-index preserved. Verdict: GREEN — Phase 1 unchanged, pending continuous fire.
- 2026-06-06 · PHASE 1 · Built Hermes Agent config contract: core/src/hermes/agent-config.schema.json (draft-07) + agent-config.js (ajv validator validateHermesAgentConfig + SAMPLE_COMPETITOR_WATCHER). Verify GREEN: sample valid; rejects missing-required, bad memory_mode, cron-without-expr, bad status. node --check clean. Branch claude/hermes-phase-1. Prod deploy deferred (dormant until Phase 4). Verdict: GREEN → advance to Phase 2.
- 2026-06-06 · PHASE 2 · Built hm-hermes artifacts on branch claude/hermes-phase-2: services/hm-hermes/{Dockerfile (FROM nousresearch/hermes-agent:latest +healthcheck +shim), shim.mjs (Phase-4 stub), .env.example, README.md (deploy runbook)} + added hm-hermes service (build, shm_size 1g, ports 8642/9119, hermes-data vol, hivemind-network, healthcheck) and hermes-data volume to docker-compose.coolify.yml. VERIFY: docker compose config valid; shim node --check OK. ⛔ BLOCKED on prod boot/deploy — needs human go (new third-party image on Hetzner + live Coolify stack edit + secrets HERMES_API_SERVER_KEY/HERMES_DASHBOARD_PASSWORD). Loop STOPPED (no re-arm) per safety rail: prod infra is human-gated. Verdict: artifacts GREEN, deploy BLOCKED.
- 2026-06-06 · PHASE 2 DEPLOY · User authorized ssh deploy. Pulled nousresearch/hermes-agent:latest on myserver; isolated smoke (:18642 → 404 = up); deployed persistent hm-hermes via docker run (--restart unless-stopped, --shm-size=1g, vol hermes-data:/opt/data, ports 8642/9119, network s0k0..._hivemind-network, API_SERVER_KEY + dashboard basic-auth generated → /opt/HIVEMIND/.hm-hermes.env chmod 600). VERIFY GREEN: :8642 HTTP 404 (gateway up), :9119 HTTP 302 (dashboard basic-auth), /opt/data state present, RESTART → :8642 up + config.yaml persisted. Standalone (not Coolify) — compose entry kept as IaC. Verdict: Phase 2 DONE → advance Phase 3 (MCP wiring).
- 2026-06-06 · PHASE 3 · Wired HiveMind MCP into hm-hermes default profile via 'hermes mcp add hivemind --url https://core.hivemind.davinciai.eu:8050/api/mcp --auth header' (master key, server-to-server MVP). Saved to /opt/data/config.yaml as Authorization: Bearer ${MCP_HIVEMIND_API_KEY} (env-ref, NO literal secret; key persisted in /opt/data/.env). 32/32 hivemind_* tools enabled; 'hermes mcp test hivemind' connects; survives container restart. VERIFY GREEN (connection + tool enumeration). Per-tenant consumer tokens deferred to Phase 6; recall+save e2e deferred to Phase 5 (needs model). Verdict: Phase 3 DONE → Phase 4 (hm-control client).
- 2026-06-06 · PHASE 4 · Built core/src/hermes/hm-control-client.js (runOnce/checkHealth/getStatus → hm-hermes:8642 /v1/chat/completions, model hermes-agent, Bearer API_SERVER_KEY; validates Phase-1 contract; timeout from safety_policy). FIXED network gap: hm-hermes was only on s0k0_hivemind but hm-core/hm-control run on 'hmtest' → connected hm-hermes to hmtest (additive). VERIFY GREEN from hm-control container: /v1/models HTTP 200 (reach+auth), runOnce round-trip OK (gateway 500 'No inference provider configured' = expected model-key gap, Phase 5). node --check clean. Module dormant until Phase 5 mounts route + deploys hm-control. Verdict: Phase 4 DONE → Phase 5 (needs model key).
- 2026-06-06 · PHASE 5 CORE · Configured hm-hermes model → Groq custom provider (base_url api.groq.com/openai/v1). gpt-oss-20b broke tool-call loops (Groq rejects reasoning_content on follow-up) → switched to llama-3.3-70b-versatile. Literal Groq key set in config (env-ref didn't resolve for model.api_key). PONG 200 verified. E2E PROVEN: instructed agent (via hm-control→gateway) to hivemind_save_memory → memory LANDED in HiveMind (id a7f4edc1, marker HERMES-E2E-1780777519, recall score 0.98) — the full hm-control→Hermes→MCP→saved+recallable loop. NOTE: agent write succeeds but conversation loop reliability shaky with 32 tools ('Failed to call a function') — reduce tool set as polish. Verdict: Phase 5 CORE GATE GREEN. STOP at Phase 6 GATE (pods-per-client) — needs explicit human go.
- 2026-06-06 · PHASE 5 POLISH · Set default-profile SOUL.md = Competitor Watcher (research→recall-dedupe→save). Configured Groq model provider. ROOT-CAUSED the reliability issue: Hermes replays assistant reasoning_content to Groq → HTTP 400 on follow-up turn (every Groq reasoning model: gpt-oss-20b, qwen3-32b; include_reasoning=false/reasoning_effort=none did NOT fix — it's Hermes replay behavior). SAVE itself works every run (proven 4×, memories landed + purged). enabled_tools allowlist tried + reverted (broke tool availability). Stabilized model=gpt-oss-20b reasoning_effort=low. Competitor Watcher FUNCTIONAL (persists findings); clean completion needs a reasoning_content-tolerant provider key or Hermes patch. Remaining: web entitlement, route, FE. STOP at Phase 6 GATE. Verdict: Phase 5 functional + caveats logged.
- 2026-06-06 · PHASE 6 ARCHITECTED (workflow wd4qoyvvi, 5 parallel design agents + synth) · Added detailed pods-per-client plan to README: hm-hermes-manager (Docker-API per-tenant containers on single host; K8s long-term). 7 ordered sub-phases 6a-6g; 6a-6e autonomous + default-off, 6f/6g human-gated (first prod tenant + scoped MCP tokens). Finish-first reuse: hm-control-client.js {baseUrl,apiKey} as-is, agent-config validator, same hm-hermes image, existing networks/secrets. STATE→6a. Verdict: GREEN, ready to execute 6a.
- 2026-06-06 · PHASE 6a · Built per-tenant runtime-spec contract (core/src/hermes/runtime-spec.schema.json + runtime-spec.js: buildRuntimeSpec/validateRuntimeSpec/deriveGatewayPort, ajv). PLACEMENT FROZEN: Node module inside hm-control (reuse hm-control-client.js+auth; rejected Python sidecar for MVP). container_host field = K8s/multi-host forward-compat. Verify GREEN: sample valid; rejects missing/bad name/status/port; deterministic+distinct ports. Branch claude/hermes-phase-6a. Dormant (deploy when wired 6e). Verdict: GREEN → 6b.
- 2026-06-06 · PHASE 6b · hermes_runtimes registry migration (core/prisma/migrations/20260606210000_hermes_runtimes). Raw-SQL table (no schema.prisma touch — drift-safe; retrieval-config.js access pattern). Applied to prod Postgres via stdin-pipe (heredoc-over-ssh failed first — consumed locally; fix = docker exec -i psql < file). Verify GREEN: table present, 0 rows, correct columns/PK/status-index, additive (no breakage). Branch claude/hermes-phase-6b. Verdict: GREEN → 6c (Docker orchestrator, local socket only).
- 2026-06-06 · PHASE 6c · Built core/src/hermes/profile-orchestrator.js (createProfile/start/stop/restart/status/deleteProfile via docker exec hm-hermes hermes -p; per-profile .env API_SERVER_PORT + SOUL; base64 file writes; child_process only). PROBE confirmed per-profile gateway on distinct port reachable cross-container at hm-hermes:<port> (hm-control→hm-hermes:18701 = 404 up, no host publish). VERIFIED live: create→startGateway(:18702 up)→status running→stop→delete→gone (throwaway profile, cleaned). Branch claude/hermes-phase-6c. Verdict: GREEN → 6d (resolver + per-tenant SOUL/mcp.json + runTask via hm-control-client).
- 2026-06-07 · PHASE 6d · profile-manager.js (resolveRuntimeUrl/ensureProfile/runTask/reconcile/destroyProfile) + orchestrator writeProfileFile. ensureProfile writes per-profile config.yaml (Groq model LITERAL key [env-ref gotcha] + HiveMind MCP) + startGateway + registry upsert (raw SQL). VERIFIED live (throwaway mgrtest): ensureProfile→gateway :18766 running→PONG via hm-control HTTP 200→registry row→full cleanup. Reuses 6c + hm-control-client unchanged. Branch claude/hermes-phase-6d. Verdict: GREEN → 6e (hm-control routes, default-off).

## 6e — hm-control /hermes/* control plane (DEPLOYED default-OFF) — 2026-06-07
- Migration 20260607000000_hermes_agents_jobs: hivemind.hermes_agents (roster/config jsonb, org-scoped) + hivemind.hermes_jobs (append-only audit). Additive raw-SQL, idempotent. Applied to prod hivemind DB via `docker exec -i postgres-s0k0… psql` stdin pipe (heredoc-over-ssh gotcha avoided). Verified: both to_regclass non-null.
- core/src/hermes/control-routes.js: handleHermesRoutes(req,res,ctx) → returns true if it owned the response. Routes: GET/POST /hermes/agents, PATCH /:id, POST /:id/{run,pause,resume}, GET /:id/{runs,logs,approvals}, POST /:id/approvals/:aid. Every req: (1) flag-gate HERMES_MANAGER_ENABLED!=='true'→404, (2) requireSession, (3) org-ownership (agent.org_id===session.orgId else 403). tenant==org. Dispatch→profile-manager.runTask + audit row; run-while-paused→409.
- profile-manager import is LAZY (dynamic import inside the run branch only) so default-OFF and server boot never load runtime-spec→ajv. Defensive even though ajv present in hm-control.
- Wired ONE delegating call in control-plane-server.js before the 404 fallthrough (NEVER server.js) + one import line.
- VERIFY: node --check both ✅. Local harness 17/17 (flag-off 404, non-hermes fall-through, no-session 401, list, create 201 + org-scoped, no-name 400, cross-org PATCH 403, patch own, pause, run-while-paused 409, run no-task 400, runs list, unknown-agent 404).
- DEPLOY: docker cp whole src/hermes (8 files) + edited control-plane-server.js into hm-control:/app; backed up server as .bak-6e; restart → "[control-plane] listening on 3000" clean. Cold-tests: /hermes/agents & /hermes/agents/x/run → 404 (flag OFF, prod unchanged); /v1/bootstrap → 200 (regression alive); HERMES_MANAGER_ENABLED unset. In-container import chain resolves (control-routes.handleHermesRoutes + profile-manager.runTask both function) → 6g flip is safe.
- Rollback: `docker exec hm-control cp /app/src/control-plane-server.js.bak-6e /app/src/control-plane-server.js && docker restart hm-control`.
- 6g (HUMAN-GATE) unchanged: first prod tenant profile + per-tenant scoped MCP token + flip HERMES_MANAGER_ENABLED.

## 6f — FE Hermes Agents page (COMMITTED, not deployed) — 2026-06-07
- FE repo: /Users/amar/HIVE-MIND/frontend/Da-vinci (separate git, branch preview/meeting-notes-dial). Commit b87358b.
- NEW src/components/hivemind/app/pages/HermesAgents.jsx — per-tenant agent roster (cards w/ Cpu icon, StatusBadge active/paused/archived), inline Run dialog (task+context → runHermesAgent, RunResult render), Pause/Resume (optimistic status), expandable Runs (listHermesRuns) + pending approvals (listHermesApprovals). CreateDialog (name + optional JSON config). Loading/error/empty + DEFAULT-OFF: listHermesAgents 404 → calm "not enabled" state (errStatus check), never error toast.
- api-client.js: +10 Hermes methods on the control-plane axios client (list/create/update/run/pause/resume/listRuns/listApprovals/decideApproval) — mirror existing /v1 method style ({data}=await this.controlPlane.<verb>).
- HiveMindApp.jsx: React.lazy(./pages/HermesAgents) + <Route path="hermes">. Sidebar.jsx + TopBar.jsx entries (Hermes Agents, /hivemind/app/hermes).
- Built via Workflow wom8844eg (READ×4 → BUILD → VERIFY). Workflow FAILED on the build agent's StructuredOutput call, but it HAD written api-client + router/sidebar/topbar edits; the page file itself was missing → I wrote HermesAgents.jsx by hand matching DigitalEmployees theme + the 6e API surface.
- VERIFY: ESLint (react-app config) clean on all 5 files (exit 0); Cpu import + PageSuspense + route placement confirmed. Skipped full CRA prod build (heavy); lint+import check sufficient.
- NOT pushed/deployed — FE deploy + backend flag flip = 6g human gate.

## STOP — 6g is HUMAN-GATED. Autonomous Hermes work (6a–6f) complete.
- 6g requires: first PROD tenant profile, per-tenant scoped MCP token, flip HERMES_MANAGER_ENABLED=true, docker.sock/secrets/resource-cap review, push+deploy Da-vinci FE. Operator action.

## 6g security review → 6h secure transport (DEPLOYED LIVE) — 2026-06-07
- USER lifted the 6g human gate ("do it all"). Security review FIRST (gates the flip).
- REVIEW FINDINGS (RED): hm-control has NO docker binary, NO /var/run/docker.sock, NO DOCKER_HOST — and is PUBLIC-FACING (3000→0.0.0.0:3002). The 6c profile-orchestrator's `docker exec hm-hermes …` could never run from there. Only fix for as-coded = mount host socket into a public container = host-root escalation. REJECTED. hm-hermes also had NO resource caps (unbounded).
- DECISION (user picked): build secure transport (6h), no socket.
- 6h: in-container management server inside hm-hermes (s6 longrun, services/hm-hermes/mgmt-server.mjs) runs the LOCAL hermes CLI (execFile, no shell) + profile file writes. Bearer-auth HERMES_MGMT_KEY (timing-safe, fail-closed), path-traversal guard, internal port 8650 NEVER published. hm-control's profile-orchestrator.js rewritten as a thin HTTP client (same exports → profile-manager untouched bar a readiness fix). profile-manager.ensureProfile got waitForGateway() (gateway start returns before API server binds → ECONNREFUSED otherwise).
- DEPLOY: built hm-hermes:6h image; recreated hm-hermes (no traffic) from it w/ HERMES_MGMT_KEY in /opt/HIVEMIND/.hm-hermes.env; appended HERMES_{MGR_URL,MGMT_KEY,API_SERVER_KEY},MCP_HIVEMIND_API_KEY,HERMES_MANAGER_ENABLED=true to COOLIFY_ENV; deployed 6h code to host bind path /opt/HIVEMIND/core/src/hermes/; recreated hm-control via `bash scripts/deploy.sh control`.
- VERIFY (live, from inside hm-control which has NO docker): pingManager ok; mgmt auth 401-no-key/ok-with-key; create+start+reach derived gateway 401-unauth/200-authed; FULL e2e ensureProfile→runTask→Groq+MCP returned "PONG", registry upsert, cleanup; flag-on /hermes/agents→401; hm-control healthy. Placeholder-key gotcha found (Hermes refuses placeholder API_SERVER_KEY).
- HARDENING: applied `docker update --cpus 4 --memory 6g` to hm-hermes (was unbounded). NOTE: not yet in a managed run script → a manual recreate would drop it.
- FOLLOW-UPS (non-blocking): kanban.db.init.lock PermissionError (root vs uid1000, cron noise); per-tenant scoped MCP tokens (still shared master); managed run script for hm-hermes; bake resource caps in.
