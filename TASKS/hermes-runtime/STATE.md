# STATE — hermes-runtime

- **Current phase:** 5 (Competitor Watcher e2e) — ✅ CORE GATE MET; ⛔ Phase 6 GATE awaits human go
- **CORE PROVEN (Phase 5 hardest gate):** Hermes agent → HiveMind MCP → hivemind_save_memory → memory persisted in HiveMind and recallable (id a7f4edc1, title "Hermes Competitor Watcher e2e", marker HERMES-E2E-1780777519, tags [hermes-e2e,competitor], recall score 0.98). Path: hm-control → hm-hermes:8642 (Bearer API_SERVER_KEY) → llama-3.3-70b (Groq) → MCP write → HiveMind. Model wired: config.yaml provider=custom, base_url=https://api.groq.com/openai/v1, model=llama-3.3-70b-versatile (gpt-oss-20b removed — Groq rejects its reasoning_content on tool-call follow-up turns), literal Groq key in config (root-only /opt/data).
- **Remaining Phase 5 polish (not core-blocking, can be a 5b):** (1) tool-calling RELIABILITY — agent write succeeds but the conversation loop sometimes errors ("Failed to call a function" with all 32 tools enabled); reduce enabled MCP tools to a small set (save/recall/web_search) via `hermes mcp configure` (needs TTY) or config edit to improve 70b reliability. (2) Build the hermes-competitor-watcher profile/distribution (SOUL + web_search/browser skills + output route). (3) Mount a thin internal route in control-plane-server.js calling hm-control-client.runOnce (FE→hm-control only) + deploy hm-control. (4) FE display.
- **Branch:** P5 = claude/hermes-phase-5.
- **Last verdict:** Phase 5 CORE GREEN (memory lands in HiveMind via Hermes MCP — the acceptance gate). Full FE→browser→shown e2e + reliability tuning remain as polish. NEXT IS THE PHASE 6 GATE.
- **Phases:** 1✅ 2✅ 3✅ 4✅ 5✅(core) · 6 ⛔ GATE pods-per-client — DO NOT START without explicit human go.
- **Do NOT:** edit core/src/server.js · git pull on prod · commit secrets · START PHASE 6 without explicit human approval (multi-tenant isolation = deliberate infra decision).
