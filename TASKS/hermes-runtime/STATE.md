# STATE — hermes-runtime

- **Current phase:** 5 (Competitor Watcher e2e) — NOT STARTED (likely needs a model API key)
- **Next concrete action:** (a) Configure a model provider for hm-hermes — set a key in /opt/data/.env (OPENROUTER_API_KEY / OPENAI_API_KEY / ANTHROPIC; prod has GROQ_API_KEY which Hermes can use via `hermes model` / custom base_url) so the agent can actually answer. (b) Build the `hermes-competitor-watcher` profile/distribution (SOUL + browser/web_search skills + output route → HiveMind memory). (c) Mount a thin internal route in control-plane-server.js that calls hm-control-client.runOnce (FE→hm-control only) + deploy hm-control (surgical, verify+rollback). VERIFY e2e: FE→hm-control→Hermes→browser/search→HiveMind MCP→memory saved + shown. If no model key available → log exactly which key is needed, STATE=BLOCKED.
- **Branch:** P1..P4 done (P4 = claude/hermes-phase-4). P5 → claude/hermes-phase-5.
- **Last verdict:** Phase 4 DONE (client). core/src/hermes/hm-control-client.js — runOnce(agentConfig,payload) → POST hm-hermes:8642 /v1/chat/completions (model hermes-agent, Bearer API_SERVER_KEY), checkHealth(), getStatus(); validates via Phase 1 contract; timeout from safety_policy. VERIFY GREEN from hm-control container: /v1/models 200 (reach+auth), runOnce round-trip sends valid request + parses response (got gateway 500 "No inference provider configured" = expected, Phase 5 model-key gap). FIXED network: hm-hermes attached to hmtest (where hm-core/hm-control live) in addition to s0k0_hivemind — reachable as http://hm-hermes:8642. Module dormant until Phase 5 mounts a route + deploys hm-control.
- **Phases:** 1✅ 2✅ 3✅ 4✅ hm-control client · 5 Competitor Watcher e2e · 6 GATE pods-per-client
- **Done:** P1 config · P2 deployed · P3 MCP wired · P4 hm-control client
- **NETWORK NOTE:** hm-hermes must be on `hmtest` (hm-core/hm-control network). Runtime `docker network connect hmtest hm-hermes` applied; re-apply if container recreated (not yet in compose IaC).
- **Do NOT:** edit core/src/server.js · git pull on prod · commit secrets · start Phase 6 before Phase 5.
