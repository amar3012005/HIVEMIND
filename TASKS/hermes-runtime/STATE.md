# STATE — hermes-runtime

- **Current phase:** 6e (hm-control /hermes/* routes — default-off) — NOT STARTED
- **ARCHITECTURE:** profiles-per-tenant (NOT pods). One shared hm-hermes container; 1 tenant = 1 profile with own gateway on a distinct API_SERVER_PORT (hm-hermes:<port>). Manager = Node module in hm-control. PROVEN through 6d.
- **Next concrete action:** 6e — add /hermes/* routes to core/src/control-plane-server.js (NEVER server.js): GET/POST /hermes/agents, PATCH /:id, POST /:id/run|pause|resume, GET /:id/runs|logs|approvals, POST /:id/approvals/:aid → call profile-manager (ensureProfile/runTask/destroyProfile/reconcile) + hm-control-client. Session-auth + org-scope on every route. hermes_jobs audit table (additive migration). Default-OFF behind HERMES_MANAGER_ENABLED=false. VERIFY: node --check; local route test + cross-org 403 + flag-off-noop; deploy default-OFF via deploy-verified.sh (rollback on RED). NO prod tenant profile creation (that's 6g).
- **Done:** Phases 1-5 (single shared container live, MCP, hm-control client, Competitor Watcher e2e). 6a runtime-spec ✅. 6b hermes_runtimes registry ✅ (prod). 6c profile-orchestrator ✅ (live-verified). 6d profile-manager ✅ (ensureProfile→PONG via hm-control→registry, live-verified). Pods/container-per-tenant path REMOVED (dead).
- **Sub-phases:** 6c ✅ · 6d ✅ · 6e hm-control routes (flag-off) · 6f FE Hermes Agents page · 6g ⛔HUMAN-GATE first prod tenant + per-tenant scoped MCP token + routing flip.
- **GOTCHAS:** model.api_key needs LITERAL key (env-ref unresolved; MCP header env-ref OK). ESM ignores NODE_PATH — symlink node_modules for host tests. Groq reasoning models 400 on reasoning_content replay in multi-turn (single-turn fine).
- **Do NOT:** edit core/src/server.js (use control-plane-server.js) · git pull on prod · create a PROD tenant profile or flip HERMES_MANAGER_ENABLED before 6g human go · commit secrets · rebuild the dropped pods path.
