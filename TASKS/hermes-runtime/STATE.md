# STATE — hermes-runtime

- **Current phase:** 6f (FE Hermes Agents page) — NOT STARTED
- **ARCHITECTURE:** profiles-per-tenant (NOT pods). One shared hm-hermes container; 1 tenant = 1 profile with own gateway on a distinct API_SERVER_PORT (hm-hermes:<port>). Manager = Node module in hm-control. PROVEN through 6e.
- **Next concrete action:** 6f — FE "Hermes Agents" page (frontend/Da-vinci) reusing Digital-Employees roster; card = tenant profile; controls map 1:1 to the 6e APIs (list/create/patch/run/pause/resume/runs/logs/approvals). Page hidden unless feature flag on; APIs already default-OFF server-side.
- **Done:** Phases 1-5 (single shared container live, MCP, hm-control client, Competitor Watcher e2e). 6a runtime-spec ✅. 6b hermes_runtimes registry ✅ (prod). 6c profile-orchestrator ✅ (live-verified). 6d profile-manager ✅ (live-verified). **6e hm-control /hermes/* routes ✅ DEPLOYED default-OFF** — control-routes.js (lazy-loads profile-manager only on dispatch) + hermes_agents/hermes_jobs migration (applied to prod hivemind DB); 17/17 local route tests; prod cold-test flag-off→404, boot clean, import chain resolves in-container; /app/src/control-plane-server.js.bak-6e kept for rollback. Pods/container-per-tenant path REMOVED (dead).
- **Sub-phases:** 6c ✅ · 6d ✅ · 6e ✅ (deployed flag-off) · 6f FE Hermes Agents page · 6g ⛔HUMAN-GATE first prod tenant + per-tenant scoped MCP token + routing flip.
- **GOTCHAS:** model.api_key needs LITERAL key (env-ref unresolved; MCP header env-ref OK). ESM ignores NODE_PATH — symlink node_modules for host tests. Groq reasoning models 400 on reasoning_content replay in multi-turn (single-turn fine).
- **Do NOT:** edit core/src/server.js (use control-plane-server.js) · git pull on prod · create a PROD tenant profile or flip HERMES_MANAGER_ENABLED before 6g human go · commit secrets · rebuild the dropped pods path.
