# STATE — hermes-runtime

- **Current phase:** 6h DONE + LIVE. HERMES_MANAGER_ENABLED=true in prod. Secure transport proven e2e. Remaining = FE visible release (user's call) + prod-hardening follow-ups.
- **ARCHITECTURE:** profiles-per-tenant (NOT pods). One shared hm-hermes container; 1 tenant = 1 profile with own gateway on a distinct API_SERVER_PORT (hm-hermes:<port>). **6h transport: hm-control (public, NO docker) → HTTP(bearer) → in-container mgmt server (s6 longrun in hm-hermes, runs LOCAL hermes CLI) → per-profile gateway. NO docker.sock anywhere.** PROVEN through 6h.
- **Next concrete action (USER decisions):** (1) FE visible release — adds a "Hermes Agents" nav item for all org users; commit b87358b ready on Da-vinci preview/meeting-notes-dial, merge to main to expose. (2) prod-hardening follow-ups: hm-hermes resource caps (was unbounded — caps applied via docker update, NOT yet baked into a managed run script → won't persist a recreate); kanban.db.init.lock PermissionError (root-owned, gateway runs uid1000 — cron noise, non-fatal); per-tenant scoped MCP tokens (still shared master key); managed run script for hm-hermes (currently manual docker run).
- **Done:** Phases 1-5 (single shared container live, MCP, hm-control client, Competitor Watcher e2e). 6a runtime-spec ✅. 6b hermes_runtimes registry ✅ (prod). 6c profile-orchestrator ✅ (live-verified). 6d profile-manager ✅ (live-verified). **6e hm-control /hermes/* routes ✅ DEPLOYED default-OFF** — control-routes.js (lazy-loads profile-manager only on dispatch) + hermes_agents/hermes_jobs migration (applied to prod hivemind DB); 17/17 local route tests; prod cold-test flag-off→404, boot clean, import chain resolves in-container; /app/src/control-plane-server.js.bak-6e kept for rollback. **6f FE Hermes Agents page ✅ COMMITTED** (Da-vinci repo commit b87358b, branch preview/meeting-notes-dial — NOT pushed/deployed; ESLint clean; page self-gates on list-404). Pods/container-per-tenant path REMOVED (dead).
- **Sub-phases:** 6c ✅ · 6d ✅ · 6e ✅ · 6f ✅ (FE committed) · 6g security-review ✅ (found docker-exec-from-public-container blocker) · 6h secure transport ✅ DEPLOYED LIVE + flag flipped.
- **GOTCHAS:** model.api_key needs LITERAL key (env-ref unresolved; MCP header env-ref OK). ESM ignores NODE_PATH — symlink node_modules for host tests. Groq reasoning models 400 on reasoning_content replay in multi-turn (single-turn fine). **Hermes refuses to bind API server if API_SERVER_KEY looks like a placeholder (use real 32-byte hex).** **gateway start returns BEFORE the API server binds (~8s) → must waitForGateway before dispatch (done in ensureProfile).** **hm-control has NO docker — never assume docker-exec works from it; use the 6h HTTP mgmt transport.** hm-hermes mgmt port 8650 is internal-only (never publish). HERMES_MGMT_KEY in /opt/HIVEMIND/.hm-hermes.env + COOLIFY_ENV.
- **ROLLBACK:** flag off → set HERMES_MANAGER_ENABLED=false in COOLIFY_ENV + `bash /opt/HIVEMIND/scripts/deploy.sh control`. hm-control code: /app/src/control-plane-server.js.bak-6e. hm-hermes: revert image to nousresearch/hermes-agent:latest via docker run (mgmt server just won't exist).
- **Do NOT:** edit core/src/server.js (use control-plane-server.js) · git pull on prod · commit secrets · rebuild the dropped pods path · mount docker.sock into hm-control (public; rejected in 6g).
