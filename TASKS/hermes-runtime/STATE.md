# STATE — hermes-runtime

- **Current phase:** 5 (Competitor Watcher) — FUNCTIONAL; polish caveats documented. Phase 6 = GATE (human go).
- **What works (proven repeatably):** Competitor Watcher SAVES findings to HiveMind via the wired MCP every run — hm-control → hm-hermes:8642 → gpt-oss-20b (Groq) → hivemind_save_memory → memory persisted + recallable. SOUL.md set (competitor-watcher persona: web research → recall-dedupe → save concrete dated named findings). Model: openai/gpt-oss-20b, reasoning_effort low. All 32 hivemind tools available (enabled_tools allowlist was tried + REVERTED — it broke tool availability: "not in request.tools").
- **KNOWN CAVEAT (reliability):** the post-save follow-up turn 400s — stock Hermes replays the assistant `reasoning_content` field to Groq, and Groq rejects `reasoning_content` on role:assistant in a follow-up request. Hits EVERY Groq reasoning-capable model (gpt-oss, qwen3-32b). The SAVE completes (turn 1); only the final "DONE" turn fails → caller sees 502 but the work is done. NOT our infra — Hermes×Groq compat. Real fixes: (a) use a reasoning_content-tolerant provider (OpenRouter/OpenAI/Anthropic — needs a paid key), or (b) a Hermes update that strips reasoning_content on replay. extra_body.include_reasoning=false did NOT resolve it.
- **Remaining (polish, not core):** (1) clean multi-turn completion — needs the provider/Hermes fix above; (2) hivemind_web_search needs the HiveMind web entitlement enabled for the agent's principal; (3) thin route in control-plane-server.js → hm-control-client.runOnce (FE→hm-control only) + deploy hm-control; (4) FE display.
- **Branch:** P5 = claude/hermes-phase-5 (repo code unchanged this round — all config is runtime on /opt/data).
- **Phases:** 1✅ 2✅ 3✅ 4✅ 5✅(functional, caveat) · 6 ⛔ GATE pods-per-client — DO NOT START without explicit human go.
- **Do NOT:** edit core/src/server.js · git pull on prod · commit secrets · start Phase 6 without human go.
