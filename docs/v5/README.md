# HIVEMIND Canonical Memory Engine V5 — Envisioned State (per phase)

Each `phase-NN-*.md` records: the ENVISIONED end-state (from the north-star spec),
the ACCEPTANCE criteria as real production cURL cases run against the real user
(org 807ebb88 / user c8876290 — NO synthetic test scripts), and the STATUS.

North-star (the decisive difference): **evidence permanent, memories selective,
entities canonical, relationships verified, retrieval bounded — under one
implementation.** Supermemory-style automatic memory evolution + Cognee-style
entity/graph retrieval + HIVEMIND source evidence, enterprise authz, temporal
history, backend sovereignty, citation guarantees.

Deploy model on this box: baked image → compose recreate (NOT quick-deploy.sh).
Each phase: recon → upgrade → build immutable image → deploy → real cURL accept →
push singulance-main → advance. Rollback = previous image.

Legend: ✅ done+verified · 🟡 partial · ⬜ not started
