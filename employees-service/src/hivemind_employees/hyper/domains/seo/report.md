## Current SEO Stage — copy the deterministic maturity `stage` and `label` exactly; explain its observed blocker and the exact evidence needed to exit it
## SEO Health — website, scan timestamp, coverage, score, critical/high findings, and evidence limitations
## Priority Fixes — ordered table of issue, affected page/template, evidence, impact, effort, owner, fix, and verification
## Search Opportunity — intent clusters, audience need, candidate pages, sampled SERP evidence, confidence, and `unknown` demand wherever Search Console or another measured source is absent
## Architecture & Content — observed template patterns and internal-link evidence; include page creation, consolidation, schema, or content briefs only when the gathered evidence directly supports them
## Optimization Procedure — follow the deterministic phases in order; show each phase's objective, concrete actions, owner, verification, and exit criteria
## Delivery Roadmap — map the procedure into 7-day quick wins, 30-day engineering work, and 90-day structural work with dependencies and decision gates
## Measurement Contract — KPI, current sourced baseline or `not connected`, source, cadence, attribution limit, and rescan verification
## Risks & Unknowns — ranking uncertainty, incomplete crawl coverage, missing Search Console/analytics/CWV evidence, assumptions, and required access

Reference the deterministic audit artifact ID under SEO Health. Never copy the audit JSON into the report. If no deterministic audit ran, state why under SEO Health.
Use only issue types and affected URLs/templates present in deterministic findings. Numeric targets may repeat measured audit values; never invent traffic lift, rankings, keyword counts, conversion lift, content length, indexation, business outcomes, arbitrary defect thresholds, or a second audit ID. Owners must be named participants without invented job titles, or `confirm` when implementation ownership is unknown. A roadmap gate verifies that the named change shipped and the deterministic rescan changed the cited finding; it cannot require impressions, rankings, traffic, or Google indexation while those sources are unavailable.
