# The production web application is hosted on Cloudflare Workers at
# next.singulancelabs.com. This file is deliberately service-free so a Docker
# Compose invocation cannot recreate a competing server-side frontend.
name: hivemind-next
services: {}
