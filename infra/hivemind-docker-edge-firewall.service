[Unit]
Description=Restrict direct Docker edge service ingress to Caddy
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/hivemind-docker-edge-firewall
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
